#!/usr/bin/env bash
set -euo pipefail

trap "echo; exit" INT
sudo apt update
sudo apt upgrade -y

sudo fwupdmgr refresh
sudo fwupdmgr update -y

sudo snap refresh

go get -u all

cargo install-update -a
